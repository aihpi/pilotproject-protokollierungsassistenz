apiVersion: apps/v1
kind: Deployment
metadata:
  name: tops2-backend
  labels:
    app: tops2-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tops2-backend
  template:
    metadata:
      labels:
        app: tops2-backend
    spec:
      runtimeClassName: nvidia
      nodeSelector:
        accelerator: a30
      containers:
        - name: backend
          image: ghcr.io/aihpi/pilotproject-protokollierungsassistenz/backend:6-llm-config-switching-gpu
          imagePullPolicy: Always
          ports:
            - containerPort: 8010
          envFrom:
            - configMapRef:
                name: tops2-backend-config
          env:
            - name: LLM_API_KEY
              valueFrom:
                secretKeyRef:
                  name: tops2-secret
                  key: LLM_API_KEY
          resources:
            limits:
              nvidia.com/gpu: "1"
            requests:
              nvidia.com/gpu: "1"
          startupProbe:
            httpGet:
              path: /health
              port: 8010
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 8010
            periodSeconds: 10
            timeoutSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8010
            periodSeconds: 30
            timeoutSeconds: 10
